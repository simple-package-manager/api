{
  "type": "postgres",
  "host": "db",
  "port": 5432,
  "username": "user",
  "password": "password",
  "database": "user",
  "synchronize": false,
  "logging": false,
  "entities": [
    "entity/**/*.{js,ts}"
  ],
  "migrations": [
    "build/migration/**/*.{js,ts}"
  ],
  "subscribers": [
    "build/subscriber/**/*.{js,ts}"
  ]
}